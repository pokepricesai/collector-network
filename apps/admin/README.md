# apps/admin — placeholder

**Do not scaffold yet.**

This directory reserves the workspace slot for the central admin / control
centre. Private application used to manage the entire network.

Will be a separate Next.js application, deployed as its own Vercel project on
a private admin domain.

Build order: see `docs/build-order.md`.
