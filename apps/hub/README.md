# apps/hub — placeholder

**Do not scaffold yet.**

This directory reserves the workspace slot for the central Collector Network
hub — the umbrella site that describes and links to each specialist site.

Will be a separate Next.js application, deployed as its own Vercel project on
the umbrella / network-level domain.

Build order: see `docs/build-order.md`.
