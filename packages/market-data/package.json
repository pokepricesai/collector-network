{
  "name": "@collector-network/market-data",
  "version": "0.0.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "lint": "echo \"(market-data) lint placeholder\" && exit 0",
    "typecheck": "tsc --noEmit",
    "build": "echo \"(market-data) build placeholder\" && exit 0",
    "test": "node --import tsx --test src/pricing.test.ts src/selection.test.ts"
  },
  "dependencies": {
    "@collector-network/database": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.10.5",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3"
  }
}
