// @collector-network/auth — package shell.
//
// Intended responsibility: shared authentication primitives usable across
// every Collector Network site (session helpers, server-side user lookup,
// cross-site sign-in flows once decided). Not implemented yet.

export const __auth_package_placeholder = true;
